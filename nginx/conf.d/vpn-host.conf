stream {
    map $ssl_preread_server_name $name {
        www0.music-tm.com openvpn;
    }
    server {
        listen 443;
        proxy_pass $name;
        ssl_preread on;
    }
    upstream openvpn {
        server ocserv:443;
    }
}